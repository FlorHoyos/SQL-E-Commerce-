# SQL-E-Commerce-
schema.sql
Defines/creates the database structure (tables + columns). 

data_validations.sql
Data quality checks: duplicates, missing timestamps, weird values, join integrity 

sales_trends.sql
Time-based sales metrics like monthly orders and monthly revenue
